---
labels: ['build']
description: 'Create a function for filtering out dependencies that are not allowed to be built'
---

API:

```ts
function createAllowBuildFunction (opts): undefined | Function;
```
